Из терминала запустить файл interface.py. Для корректной работы приложения необходимо создать файл
  .env в корневой папке проекта
с следующим содержимым:

# Пароль postgresql
PASSWORD = '***'

# Имя базы данных
db_name = '***'

#токен группы
access_token = '***'

#токен приложения(пользователя)
user_token = '***'

#id группы
group_id = '***'
